{
  "name": "@coderpp/yunxiao-cli",
  "version": "0.1.2",
  "description": "A CLI for Yunxiao Codeup merge request review and merge workflows.",
  "type": "module",
  "bin": {
    "yunxiao": "dist/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pack:dry-run": "npm pack --dry-run",
    "prepack": "npm run build",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.17"
  },
  "files": [
    "dist/src",
    "README.md"
  ]
}
